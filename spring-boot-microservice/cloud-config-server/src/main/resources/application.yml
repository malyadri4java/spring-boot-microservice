server:
  port: 9393

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/malyadri4java/config-server-eureka-client
          clone-on-start: true
